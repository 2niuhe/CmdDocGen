# Command: cinder help revert-to-snapshot

usage: cinder revert-to-snapshot <snapshot>

Revert a volume to the specified snapshot.

Positional Arguments:
  <snapshot>  Name or ID of the snapshot to restore. The snapshot must be the
              most recent one known to cinder.
