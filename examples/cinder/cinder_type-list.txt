# Command: cinder help type-list

usage: cinder type-list [--filters [<key=value> ...]]

Lists available 'volume types'. (Only admin and tenant users will see private
types)

Options:
  --filters [<key=value> ...]
                        Filter key and value pairs. Admin only. (Supported by
                        API version 3.52 and later)
